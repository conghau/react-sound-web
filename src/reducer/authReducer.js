/**
 * Created by hautruong on 7/7/18.
 */

const initialState = {
  auth: {}
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
